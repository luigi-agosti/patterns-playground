package com.luigiagosti.learning.pattern.behavioral.interpreter;

/**
 * Given a language, define a representation for its grammar
 * along with an interpreter that uses the representation to 
 * interpret sentences in the language. 
 * 
 * Declare operation that is common to all nodes in the abstract 
 * syntax tree.
 */
public class AbstractExpression {

    public void interpret(Context context) {
        
    }
}
